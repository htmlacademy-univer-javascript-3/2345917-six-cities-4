export enum MapClassify {
  MainMapChapter = 'cities__map map',
  PropertyMapChapter = 'offer__map map',
}

export enum AppClasses {
  ImagePackingProperty = 'near-places__image-wrapper place-card__image-wrapper',
  ImagePackingMain = 'cities__image-wrapper place-card__image-wrapper',
  NoteProperty = 'near-places__card place-card',
  NoteMain = 'cities__card place-card',
}
